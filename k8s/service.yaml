apiVersion: v1
kind: Service
metadata:
  name: devops-task-api
  labels:
    app: devops-task-api
spec:
  type: ClusterIP
  selector:
    app: devops-task-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
